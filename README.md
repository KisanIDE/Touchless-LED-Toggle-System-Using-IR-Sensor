# Touchless-LED-Toggle-System-Using-IR-Sensor
Touchless LED Toggle System using an IR sensor and Arduino. When an object is detected, the system toggles the LED ON/OFF without physical contact. It also uses a detection flag to prevent repeated triggers. Serial Monitor shows real-time status of detection and LED state for easy debugging and learning.
